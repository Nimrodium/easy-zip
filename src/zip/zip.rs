// use std::path::{Path, PathBuf};
// use zip::write::
// fn zip(srcs: &[PathBuf], target: &Path) -> Result<(), String> {
//     // if !src.is_dir() {
//     return Err(format!("{src:?} is not a directory"));
// }

// // zip_extensions::zip_c
// if srcs.len() == 1 && srcs[0].is_dir() {
//     zip_extensions::zip_create_from_directory(&target.to_path_buf(), &srcs[0])
//         .map_err(|e| e.to_string())?;
// } else {

// };
//     Ok(())
// }
